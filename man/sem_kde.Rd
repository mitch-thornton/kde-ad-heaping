% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baselines.R
\name{sem_kde}
\alias{sem_kde}
\title{SEM de-heaping via the real Kernelheaping package}
\usage{
sem_kde(y, D, grid, burnin = 5, samples = 10)
}
\arguments{
\item{y}{heaped (rounded) sample.}

\item{D}{grid width.}

\item{grid}{evaluation grid (equally spaced).}

\item{burnin, samples}{SEM burn-in and sample counts passed to \code{Kernelheaping::dheaping}.}
}
\value{
Density values on \code{grid}, or \code{NULL} if \pkg{Kernelheaping} is absent.
}
\description{
Wrapper around the stochastic expectation-maximization estimator of Gross and Rendtel
(the \pkg{Kernelheaping} package), interpolated to \code{grid}. Requires that package;
returns \code{NULL} if it is not installed.
}
\examples{
grid <- seq(-6, 6, length.out = 512)
if (requireNamespace("Kernelheaping", quietly = TRUE))
  f <- sem_kde(0.5 * round(rnorm(800) / 0.5), 0.5, grid)
}
