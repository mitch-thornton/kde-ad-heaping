% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adheaping.R
\name{heap_fraction}
\alias{heap_fraction}
\title{Estimate the heaped fraction from the comb tooth height}
\usage{
heap_fraction(y, D, grid)
}
\arguments{
\item{y}{heaped (rounded) sample.}

\item{D}{grid width.}

\item{grid}{evaluation grid (equally spaced).}
}
\value{
The estimated heaped fraction in \code{[0, 1]}.
}
\description{
Under partial heaping the leading tooth has height about \code{p^2} over the residue floor,
so \code{phat = sqrt(S_peak - floor)}.
}
\examples{
grid <- seq(-6, 6, length.out = 2048)
x <- rnorm(4000); heaped <- runif(4000) < 0.4
y <- x; y[heaped] <- round(x[heaped])
heap_fraction(y, 1.0, grid)
}
