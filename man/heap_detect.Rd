% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detector.R
\name{heap_detect}
\alias{heap_detect}
\title{Detect the rounding grid as a group-matched atom in the spectral basis}
\usage{
heap_detect(y, span = NULL, Mgrid = 512, cand_K = c(8, 16, 24, 32, 48, 64, 96))
}
\arguments{
\item{y}{heaped sample.}

\item{span}{numeric length-2 range for the internal analysis grid (defaults from the data).}

\item{Mgrid}{internal grid size (default 512).}

\item{cand_K}{candidate replica periods in spectral bins.}
}
\value{
A list with \code{D_hat} (recovered grid, or NA), \code{K_hat}, \code{detected}
  (logical), and \code{defects} (symmetry defect per candidate period).
}
\description{
Higher-order comb detection: the symmetry defect of the shift-by-\code{K} quotient group on
the covariance of the power spectrum (a fourth-order statistic) is minimized at the true
replica period; the smallest period with low defect (the largest passing cyclic subgroup) is
the detected grid.
}
\examples{
set.seed(1)
x <- ifelse(runif(4000) < 0.5, rnorm(4000, -1.2, 0.5), rnorm(4000, 1.2, 0.5))
heap_detect(0.8 * round(x / 0.8), span = c(-12.8, 12.8))$D_hat
}
