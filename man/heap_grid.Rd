% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adheaping.R
\name{heap_grid}
\alias{heap_grid}
\title{Recover the rounding grid from the leading spectral replica}
\usage{
heap_grid(y, grid, near = NULL)
}
\arguments{
\item{y}{heaped (rounded) sample.}

\item{grid}{evaluation grid (equally spaced).}

\item{near}{optional grid hint; if supplied the tooth is located in a frequency window
around \code{2*pi/near} (a verification that the comb sits at the expected grid); if NULL
the first strong tooth beyond the signal lobe is taken (fully blind).}
}
\value{
The recovered grid width \code{Dhat} (or NA if no tooth is found).
}
\description{
The leading tooth of the power spectrum sits at \code{2*pi/D}, so the grid is
\code{Dhat = 2*pi / w_peak}.
}
\examples{
grid <- seq(-6, 6, length.out = 2048)
x <- ifelse(runif(4000) < 0.5, rnorm(4000, -1.2, 0.5), rnorm(4000, 1.2, 0.5))
heap_grid(0.5 * round(x / 0.5), grid, near = 0.5)
}
