% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adheaping.R
\name{adkde}
\alias{adkde}
\title{Combined AD heaping-KDE: band-capacity gate selects de-heap / superposition / iterated.}
\usage{
adkde(y, D, grid, rho_lo = 0.06, rho_hi = 0.14, seed = 20260627)
}
\arguments{
\item{y}{heaped (rounded) sample.}

\item{D}{grid width.}

\item{grid}{evaluation grid (equally spaced).}

\item{rho_lo, rho_hi}{band-capacity thresholds selecting the component.}

\item{seed}{random seed passed to the superposition components.}
}
\value{
Density values on \code{grid}, with attributes \code{pick} (the selected component)
  and \code{rho} (the band-capacity statistic).
}
\description{
The single tuning-free estimator. A band-capacity statistic (de-heaped in-band power in
the outer quarter of the identifiable band) selects the de-heaping estimator when the
density fits inside the band, the superposition when it exceeds the band, and the
iterated superposition at the coarsest grids.
}
\examples{
grid <- seq(-6, 6, length.out = 1024)
x <- ifelse(runif(2000) < 0.5, rnorm(2000, -1.2, 0.5), rnorm(2000, 1.2, 0.5))
f <- adkde(0.5 * round(x / 0.5), 0.5, grid)
attr(f, "pick")
}
